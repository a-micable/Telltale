# Optional runtime config (read by telltale::Logger via getenv).
# Copy to `.env` only if your shell tooling expects the file; the binary
# reads process environment variables, not a dotenv file.
TELLTALE_LOG_LEVEL=info
